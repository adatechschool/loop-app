export const ArrowBackIcon = ({ boxSize, ...props }: any) => (
  <span data-testid="arrow-back-icon" {...props}>←</span>
);

export const EditIcon = (props: any) => (
  <span data-testid="edit-icon" {...props}>✏️</span>
);

export const DeleteIcon = (props: any) => (
  <span data-testid="delete-icon" {...props}>🗑️</span>
);

export const HamburgerIcon = (props: any) => (
  <span data-testid="hamburger-icon" {...props}>☰</span>
);